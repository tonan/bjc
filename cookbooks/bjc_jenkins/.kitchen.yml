---
driver:
  name: vagrant
  network:
    - ["forwarded_port", {guest: 8080, host: 8080}]

provisioner:
  name: chef_zero
  require_chef_omnibus: 12

platforms:
  - name: centos-7.1

suites:
  - name: default
    run_list:
      - recipe[bjc_jenkins::default]
    attributes:
